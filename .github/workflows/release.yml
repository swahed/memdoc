name: Build and Release

on:
  push:
    tags:
      - 'v*.*.*'  # Triggers on version tags like v1.0.0, v1.1.0, etc.

permissions:
  contents: write  # Required to create releases and upload assets

jobs:
  build-and-release:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build executable
        run: python build.py
        env:
          GITHUB_REF_NAME: ${{ github.ref_name }}

      - name: Get version from tag
        id: get_version
        run: |
          $VERSION = "${{ github.ref_name }}" -replace '^v', ''
          echo "VERSION=$VERSION" >> $env:GITHUB_OUTPUT
          echo "Building version: $VERSION"

      - name: Install Inno Setup
        run: choco install innosetup -y --no-progress

      - name: Build installer
        run: |
          $VERSION = "${{ steps.get_version.outputs.VERSION }}"
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss /DMyAppVersion=$VERSION

      - name: Create Release with GitHub CLI
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $VERSION = "${{ steps.get_version.outputs.VERSION }}"

          # Create release notes
          $NOTES = @"
          # MemDoc $VERSION

          ## Was ist neu?

          <!-- Fuege hier die Aenderungen ein -->

          ## Installation

          1. Lade ``MemDoc-Setup.exe`` herunter
          2. Fuehre die Datei aus
          3. Folge dem Installationsassistenten
          4. MemDoc wird nach der Installation automatisch gestartet

          ## Hinweise

          - Windows kann eine Sicherheitswarnung anzeigen (da die .exe nicht signiert ist). Klicke auf "Weitere Informationen" -> "Trotzdem ausfuehren"
          - Speichere deine Daten in einem OneDrive-Ordner fuer automatisches Backup
          - Updates werden automatisch im Programm angeboten

          ## Changelog

          Siehe vollstaendige Aenderungen: [CHANGELOG.md](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md)

          ---

          Automatisch erstellt mit GitHub Actions
          "@

          # Create release and upload installer
          gh release create "${{ github.ref_name }}" `
            ./dist/MemDoc-Setup.exe `
            --title "MemDoc $VERSION" `
            --notes "$NOTES"
